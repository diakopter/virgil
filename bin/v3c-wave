#!/bin/bash
BIN=$(dirname ${BASH_SOURCE[0]})
BIN=$(cd $BIN && pwd)
RT=$(cd $BIN/../rt/ && pwd)
V3C=${V3C:=$BIN/v3c}
N=$RT/native
$V3C -heap-size=100m -target=wave -rt.gc -rt.gctables "$@" $RT/wave/*.v3 $N/NativeGlobalsScanner.v3 $N/NativeFileStream.v3 $RT/gc/*.v3
