var count: Array<int>;
def main(a: Array<string>) -> int {
	count = [a.length + 100];
        while (count[0]-- >= 0) {
        	var a = Array<byte>.new(4096);
                System.puti(count[0]);
                System.ln();
        }
	return 0;
}
